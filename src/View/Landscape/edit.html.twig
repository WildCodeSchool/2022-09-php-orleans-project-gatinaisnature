{% extends 'layoutAdmin.html.twig' %}

{% block stylesheet %}
	<link rel="stylesheet" href="/assets/css/landscape.css">
{% endblock %}

{% block title %}
	{{parent()}}
	| Paysages
{% endblock %}

{% block content %}
<h1>Editer Paysage admin</h1>
<ul>
{% for error in errors %}
  <li>{{error}}</li>
{% endfor %}
</ul>
<form action="" method="post" enctype="multipart/form-data" class="container">
   <input type="hidden" value="{{ event.id }}" name="id">
    <div class="mb-3">	
      <label for="title" class="form-label">Titre :</label>
      <input type="text" name="title" id="title" class="form-control" value ="{{landscape.title}}" placeholder="Titre à renseigner">
    </div>
    </div class="mb-3">
      <label for="date" class="form-label">Date :</label>
      <input type="date" name="date" id="date" class="form-control" value ="{{landscape.date}}" placeholder="Ajouter Descriptif">
    </div>
    </div class="mb-3">
      <label for="description" class="form-label">Description :</label>
      <input type="text" name="description" id="description" class="form-control" value ="{{landscape.description}}" placeholder="Ajouter Descriptif">
    </div>
    </div class="mb-3">
      <label for="cost" class="form-label">Coût :</label>
      <input type="number" step=".01" name="cost" id="cost" class="form-control" value ="{{landscape.cost}}" placeholder="Ajouter Coût">
    </div>
    <div class="mb-3">
      <label for="picture" class="form-label">Image :</label>
      <input type="file" name="picture" id="picture" value ="{{landscape.picture}}" class="form-control">
    </div>
    <div>
			{% if landscape.picture == null %}
				<img class="eventImg rounded" src="/assets/images/imgPlaceHolder.jpg" alt="eventPicture" srcset="">
			{% else %}
				<img class="eventImg rounded" src={{ landscape.picture|trim('.')}} alt="eventPicture" srcset="">
			{% endif %}
    </div>
    <button class="btn btn-primary">Mettre à jour</button>
</form>
{% endblock %}